# Python
__pycache__/
**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
**/pyvenv.cfg

# Virtual envs / secrets
/.env
.env.*
!.env.example
/.venv
/venv

# Coverage / test outputs (CI regenerates these)
.coverage
.coverage.*
htmlcov/
coverage.xml
test-results.xml

# Build / packaging
dist/
build/
*.mcpb

# IDE
.vscode/settings.json
settings.json

# Claude local notes (not shipped)
claude/
.claude/
**/claude/
**/.claude/
*.claude
claude.json
.claude.json
CLAUDE.md
personal comments.md

# One-off sprint baselines (local only)
v3.0/
v4.0/
Testing/

# graphify — keep graphify-out/ tracked; ignore caches + root temp scripts
.graphify_*
graphify-out/cache/
